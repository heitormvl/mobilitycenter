@page "/login"
@inject NavigationManager Nav

<div style="background:var(--bg); min-height:100%;">
    <StatusBar />

    <div style="padding:20px 28px 48px;">
        <!-- Logo -->
        <div style="text-align:center; padding:28px 0 36px;">
            <div style="width:72px; height:72px; border-radius:22px; background:var(--brand); margin:0 auto 16px; display:flex; align-items:center; justify-content:center; box-shadow:0 8px 28px rgba(125,17,40,0.35);">
                <i class="fa-solid fa-bicycle" style="font-size:32px; color:white;"></i>
            </div>
            <h1 style="font-size:26px; font-weight:800; color:var(--text); font-family:var(--font); letter-spacing:-0.03em; margin-bottom:6px;">MobilityCenter</h1>
            <p style="font-size:14px; color:var(--text-4); font-family:var(--font);">Micromobilidade colaborativa</p>
        </div>

        <!-- Form -->
        <div style="display:flex; flex-direction:column; gap:16px; margin-bottom:24px;">
            <div>
                <label style="display:block; font-size:13px; font-weight:500; color:var(--text-2); margin-bottom:8px; font-family:var(--font);">E-mail</label>
                <WfInput Placeholder="seu@email.com" IconClass="fa-envelope" />
            </div>
            <div>
                <label style="display:block; font-size:13px; font-weight:500; color:var(--text-2); margin-bottom:8px; font-family:var(--font);">Senha</label>
                <WfInput Placeholder="••••••••" IconClass="fa-lock" />
            </div>
            <div style="text-align:right;">
                <span style="font-size:13px; color:var(--brand); font-weight:600; cursor:pointer; font-family:var(--font);">Esqueci minha senha</span>
            </div>
        </div>

        <BtnP Style="margin-bottom:20px;" OnClick="@(() => Nav.NavigateTo("/"))">Entrar</BtnP>

        <!-- Divider -->
        <div style="display:flex; align-items:center; gap:14px; margin-bottom:20px;">
            <div style="flex:1; height:1px; background:var(--border);"></div>
            <span style="font-size:12px; color:var(--text-4); font-family:var(--font); font-weight:500;">OU</span>
            <div style="flex:1; height:1px; background:var(--border);"></div>
        </div>

        <!-- Google -->
        <BtnO>
            <svg width="18" height="18" viewBox="0 0 18 18" style="flex-shrink:0;">
                <path d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844a4.14 4.14 0 01-1.796 2.716v2.259h2.908c1.702-1.567 2.684-3.875 2.684-6.615z" fill="#4285F4"/>
                <path d="M9 18c2.43 0 4.467-.806 5.956-2.18l-2.908-2.259c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 009 18z" fill="#34A853"/>
                <path d="M3.964 10.71A5.41 5.41 0 013.682 9c0-.593.102-1.17.282-1.71V4.958H.957A8.996 8.996 0 000 9c0 1.452.348 2.827.957 4.042l3.007-2.332z" fill="#FBBC05"/>
                <path d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 00.957 4.958L3.964 7.29C4.672 5.163 6.656 3.58 9 3.58z" fill="#EA4335"/>
            </svg>
            Continuar com Google
        </BtnO>

        <!-- Sign up -->
        <div style="text-align:center; margin-top:32px; font-size:15px; font-family:var(--font);">
            <span style="color:var(--text-3);">Não tem conta? </span>
            <span style="color:var(--brand); font-weight:600; cursor:pointer;">Criar conta</span>
        </div>
    </div>
</div>
